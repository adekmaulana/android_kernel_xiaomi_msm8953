#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/lxcfs" ] || [ -e "/etc/init/lxcfs.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d lxcfs stop || exit $?
fi
# End automatically added section
